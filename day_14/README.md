# Day 14 – Exceptions & Error Handling (Udemy Day 30)

**Udemy days to assign:**

* **Day 30** – try/except/finally, raising exceptions, JSON in password manager.

**Robotics project (replace Password Manager enhancements):**
🛠 **Robust Robot Config Loader**

* Load robot config from a JSON file.
* Handle missing file, bad JSON, missing keys with helpful error messages.
* Fallback to safe defaults when config is invalid.

**1-hour lesson:**

* Discuss real-world necessity of robust robotics code (sensors may fail, bad configs).
* Live-wire a simple JSON loader with `try/except`.

**1-hour work:**

* Students write **Config Loader** and deliberately break the JSON to see errors.
* Stretch: log errors to a text file for investigation.

