# Day 7 – Intro to OOP (Udemy Day 16–17)

**Udemy days to assign:**

* **OOP section (before Coffee Machine in OOP)** – classes & objects, attributes/methods, PyPI
* **Day 17** – Creating your own class (Quiz project).

**Robotics project (replace Coffee Machine / Quiz):**
🛠 **`Robot` Class – Sensors & Actions**

* Create a `Robot` class with attributes like `name`, `battery`, `position`.
* Methods: `move()`, `rotate()`, `consume_battery()`, `report_status()`.

**1-hour lesson:**

* Explain why we model robot as a class.
* Live-code a simple `Robot` class and instantiate multiple robots.

**1-hour work:**

* Students add more methods and attributes to `Robot`.
* Stretch: implement `charge()` and `is_operational` property.

